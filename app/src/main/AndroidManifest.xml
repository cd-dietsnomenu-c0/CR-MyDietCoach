<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wsoteam.mydietcoach">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!--optional-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!--optional-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!--optional-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!--optional-->
    <uses-permission android:name="android.permission.VIBRATE" /><!--optional-->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" /> <!--optional-->
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name="com.wsoteam.mydietcoach.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<activity
            android:name="com.wsoteam.mydietcoach.MainActivity"
            android:theme="@style/MainActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->

        <activity
            android:name="com.wsoteam.mydietcoach.SplashActivity"
            android:theme="@style/Theme.AppCompat.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".tracker.LoadingActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".profile.favorites.FavoritesActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.wsoteam.mydietcoach.diets.list.old.inside.ActivityListItems"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".diets.list.old.OldDietsActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name="com.wsoteam.mydietcoach.diets.list.old.inside.article.ActivityArticle" />

        <activity
            android:name=".calculators.ActivityCalculatorSPK"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".calculators.ActivityCalculatorBrok"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".calculators.ActivityCalculatorIMT"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".calculators.ActivityCalculatorLorenc"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.wsoteam.mydietcoach.diets.list.modern.article.DietAct"
            android:theme="@style/NewDiets" />

        <activity
            android:name="com.wsoteam.mydietcoach.diets.list.modern.NewDietsListActivity"
            android:theme="@style/NewDiets" />

        <receiver android:name=".common.notifications.TrackerAlarmReceiver"/>
        <receiver android:name=".common.notifications.ReactAlarmReceiver"/>

        <receiver android:name=".common.notifications.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>

        <service
            android:name=".common.notifications.BootNotificationService"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <service
            android:name=".services.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_key" />


        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_small_notification" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="@string/authorise_file_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

    </application>

</manifest>